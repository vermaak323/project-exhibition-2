[phases.setup]
nixPkgs = ["nodePackages.npm", "nodejs", "python311", "gcc"]

[phases.install]
cmds = ["npm install", "python3 -m pip install -r requirements.txt"]

[start]
cmd = "node backend/index.js"
