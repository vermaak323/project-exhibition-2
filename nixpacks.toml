[phases.setup]
nixpkgs = ["nodePackages.npm", "nodejs", "python311", "python311Packages.pip", "gcc"]

[phases.install]
cmds = ["npm install", "cd backend && npm install", "python3 -m pip install --upgrade pip", "python3 -m pip install -r requirements.txt"]

[start]
cmd = "node backend/index.js"
