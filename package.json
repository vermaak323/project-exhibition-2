{
  "name": "price-prediction-project-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "frontend:dev": "npm run dev -w frontend",
    "backend:dev": "npm run dev -w backend",
    "start": "npm run start -w backend",
    "build": "npm run build -w frontend"
  }
}
